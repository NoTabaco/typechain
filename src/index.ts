const sayKimchiTaste = (name: string, age: number, taste: string): string => {
  return `${name}, this is ${age} year(s), taste is ${taste}!`;
};

console.log(sayKimchiTaste("Kimchi", 3, "Good"));

export {};
